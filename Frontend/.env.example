# Base URL of the Django API (note the /api suffix)
VITE_API_BASE_URL=http://127.0.0.1:8000/api

# Origin of the Django server, no path (used for /accounts/... SSO/login routes)
VITE_API_ORIGIN=http://127.0.0.1:8000
